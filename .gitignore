# Python
backend/venv/
backend/venv311/
backend/__pycache__/
**/__pycache__/
*.pyc

# 学習済みMLモデル: train_model.py で生成される派生物なのでコミットしない。
# 未学習時はpredictor.pyが簡易ヒューリスティックにフォールバックする（REQUIREMENTS_v2.md 2.2参照）
backend/model/

# 環境変数・秘密情報
.env
backend/.env
frontend/.env
!.env.example

# SSH秘密鍵（Oracle CloudのVMアクセス用）
keys/

# 注: nikkei225.json は起動時のフォールバック用シードデータとして意図的にコミットする
# （初回起動〜6時間ごとのmaster_data_updater実行までの間、銘柄一覧が空にならないようにするため）

# エディタ
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Docker / DB永続化データはボリューム管理のためリポジトリには含めない
pgdata/

# frontend/package-lock.json はコミットしない。
# Windowsで生成するとLinux向けのネイティブ依存(@emnapi/*等)が欠落し、
# Cloudflareのビルド環境(Linux)でnpm ciが失敗するため（npm installに委ねる）
frontend/package-lock.json
